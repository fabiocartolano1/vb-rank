name: '[Smart Update] FUFC D3 Matin'

on:
  schedule:
    # Tous les jours à 20h
    - cron: '0 20 * * *'
  workflow_dispatch: # Permet de lancer manuellement

jobs:
  update-fufc:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📚 Install dependencies
        run: npm ci

      - name: ⚽ Run FUFC smart update (Client SDK)
        run: npm run smart:update:fufc
